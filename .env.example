# App Environment Variables Example
# Copy this file to .env or .env.local for local development settings.

# Cloud Run / Local Socket.io Server URL
VITE_SERVER_URL=http://localhost:8080

# Standard Redis Connection URL (Optional - Fallback to LocalStorage if empty)
VITE_REDIS_URL=redis://localhost:6379
